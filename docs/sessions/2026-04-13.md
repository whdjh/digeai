# 2026-04-13

## 15:57 — `.claude/` 하네스 + `docs/` 구조 초기 셋업

### 한 일
- `.claude/settings.json` 작성: permissions(허용/거부/물어봄), language=korean, statusLine, spinnerTipsOverride, PreToolUse 훅
- `.claude/hooks/scan-vite-secrets.sh` 추가: VITE_ prefix가 시크릿명에 붙으면 차단 (jq + grep 정규식 기반). 4가지 케이스 pipe-test 통과
- `.claude/statusline.sh` 추가: `📰 digeai · <model> · ⎇ <branch>` 형태
- `.claude/commands/` 슬래시 커맨드 3개:
  - `/log-session [메모]` — 오늘 세션 로그 append
  - `/new-adr <제목>` — 새 ADR 스캐폴딩 + 인덱스 갱신
  - `/test-pipeline morning|evening` — 파이프라인 로컬 실행 + 분석
- `.claude/agents/secret-auditor.md` — 시크릿 누출 패턴 5종 검사 에이전트
- `docs/` 구조: `decisions/`, `sessions/`, `runbooks/` + 각 README + 첫 ADR(0001)
- `docs/runbooks/` 2종: Resend 한도 초과, RSSHub 인스턴스 장애
- `CHANGELOG.md` 초기화 (Keep a Changelog 형식)
- `.gitignore`에 `.env` 계열 누락 발견 → 보강 (`.env`, `.env.local`, `.env.*.local`, `!.env.example`, `.claude/settings.local.json`)

### 결정
- **settings.local.json 분리 안 함** — 1인 프로젝트라 분리 오버헤드 불필요. 단일 `settings.json`만 사용. (ADR로 빼지 않음 — 트리비얼)
- **language=korean** — 한국어 응답 기본
- **defaultMode=default** — acceptEdits 같은 자동승인 모드는 비활성. 보안 가드를 약화시킬 수 있음
- **시크릿 보호는 permissions(.env 차단) + hook(VITE_ 패턴 차단) 이중 방어** — 경로 검사는 permissions, 콘텐츠 검사는 hook으로 역할 분리

### 커밋 (의미 단위 9분할)
- `whdjh <wngns9807@gmail.com>` 로컬 user 설정 (글로벌 건드리지 않음)
- `e6ce702` chore: 프로젝트 초기 스캐폴딩 + .env .gitignore 보강
- `8de0f10` chore(claude): .claude 하네스 기본 구성
- `33436dd` feat(claude): 슬래시 커맨드 3종 + secret-auditor 에이전트
- `21a5809` docs: docs/ 히스토리 구조 + CHANGELOG
- `f06b387` chore: Phase 0 셋업 — 의존성·툴링·환경변수 골격
- `057a87e` feat(pipeline): 뉴스 수집·요약·발송 전체 파이프라인 구현
- `64cadda` feat(api): 구독 API + DB 초기화 스크립트
- `8b5d119` ci: GitHub Actions 뉴스레터 워크플로우
- `05cb2f1` feat(web): 랜딩 페이지 — 이메일 구독 폼 + 토스트
- (이번 커밋) docs: 배포 runbook + ADR-0002 + CHANGELOG/세션로그 갱신

---

## 16:30 — 풀 자율 모드 전환 + 전체 시스템 구현

### 컨텍스트
사용자 지시: "내가 개발에 손을 안 대고 알아서 돌아가는 시스템", "나한테 허락도 안 받아도 되는 그런 프로젝트". 풀 자율 모드.

### 한 일
- **자율 모드 메모리 저장** — `~/.claude/projects/.../memory/feedback_autonomy.md`
- **`.claude/settings.json` 변경** — `defaultMode: 'default'` → `'acceptEdits'` (자율 실행)
- **Phase 0 셋업** — 의존성 (rss-parser, @google/genai, resend, @libsql/client, dotenv, tailwindcss v4) 일괄 설치, vite.config에 Tailwind 플러그인, netlify.toml, .env.example, 디렉토리 골격
- **Phase 1 — 5개 에이전트 병렬 디스패치 시도**: pipeline 수집 / pipeline 출력 / frontend / netlify function / GH Actions
  - **결과**: 5개 모두 권한 거부로 실패. 일반 서브에이전트는 메인 세션과 별도 권한 스코프(Write/Edit/Bash 모두 deny). 자율 모드 settings 변경은 현재 세션에 미반영(watcher가 시작 시점에 .claude 디렉토리 없었음).
  - **대응**: 메인에서 16개 파일 직접 작성 (pipeline 13 + netlify 2 + GH workflow 1)
- **Phase 2 — 검증**:
  - `node --check` 14개 파일 syntax 통과
  - `eslint.config.js` 갱신 (Node 환경 globals를 pipeline/netlify/scripts에 적용)
  - `npm run lint` 클린
  - `npm run build` 성공 (197KB JS / 24KB CSS)
  - `renderEmail` smoke test (subject 포맷 + items + trend 포함 확인)
  - `normalizeUrl` + `dedup` smoke test (UTM 제거, 호스트 소문자, 중복 제거)
  - 모든 모듈 export 시그니처 확인
- **운영 docs** — 첫 배포 runbook, ADR-0002 (Gemini 단일 호출 결정)

### 결정
- **풀 자율 모드 운영 원칙**: 라우틴 결정은 묻지 않고 진행, 위험 액션(git push, .env 직접 수정 등)만 확인 — 메모리에 저장
- **defaultMode = acceptEdits** — 파일 편집은 자동 승인 (다음 세션부터 적용)
- **Gemini 청킹 임계값 30** — 전체 한 호출 우선, 30 초과 시만 청크 (ADR-0002)
- **Resend idempotencyKey 포맷**: `digeai/{session}/{KST_YYYYMMDD}/{email}` — KST 기준 날짜로 안정성 확보 (UTC 자정 부근 cron이 어느 KST 날짜에 매핑될지 모호함 회피)
- **Netlify Functions v2 (Web Request/Response)** — 구식 event/context 대신
- **Tailwind v4 + `@tailwindcss/vite`** — 설정 파일 없이 `@import "tailwindcss"` 한 줄
- **rate limit은 in-memory 단순 Map** — cold start마다 리셋되는 건 v1에서 OK (간단함 우선)
- **eslint config 분할** — src/는 browser globals, pipeline/netlify/scripts는 node globals

### 발견
- **서브에이전트는 메인과 다른 권한 스코프** — 일반 general-purpose 에이전트 5개 모두 Write/Edit/Bash 거부됨. 다음번엔 직접 작성하거나 권한 명시적으로 부여하는 방법 필요. 병렬화 이점 없이 시간만 소비.
- 메인 세션에서 직접 작성도 충분히 빠름 (16개 파일 ~10분)

### 변경 파일 (Phase 1 + 2 누적)
- pipeline/lib/{article,url,retry}.js, pipeline/config/sources.js, pipeline/sources/{index,rss,rsshub}.js, pipeline/{dedup,summarize,render,send,main}.js, pipeline/templates/email.html
- netlify/functions/subscribe.js
- scripts/init-db.js
- .github/workflows/newsletter.yml
- src/{App,main}.jsx, src/components/{SubscribeForm,Toast}.jsx, src/index.css, index.html
- 삭제: src/App.css, src/assets/*, public/icons.svg
- eslint.config.js (Node 환경 추가)
- package.json (deps + npm scripts: db:init, pipeline:morning, pipeline:evening)
- vite.config.js (Tailwind 플러그인)
- netlify.toml, .env.example (신규)
- docs/runbooks/first-deployment.md (신규)
- docs/decisions/0002-batch-summarize-in-one-gemini-call.md (신규)
- docs/{decisions,runbooks}/README.md 인덱스 갱신
- CHANGELOG.md 갱신
- .claude/settings.json (defaultMode = acceptEdits)

### 이슈 / 다음 액션
- [x] ~~첫 배포~~ → 부분 진행: `.env` 채움 + DB 초기화 + 로컬 smoke test 8회
- [ ] **GitHub push + Netlify 배포** (다음 세션)
- [ ] (장기) Resend 한도 임박 시 ADR로 유료 전환 결정 기록

---

## 16:50 — 첫 실 운영 검증 + 8회 메일 smoke test (사용자 피드백 루프)

### 컨텍스트
사용자가 `.env` 채움 → DB 초기화 → 파이프라인 실 운영 검증 + 메일 발송 8회 반복하며 점진적 개선. "다음 작업은 다음에"로 마무리.

### 한 일

**Phase 1 — 첫 운영 시도 (Gemini 503)**
- `npm run db:init` ✅ — Turso subscribers 테이블 생성
- `npm run pipeline:evening` 시도 → Gemini 503 "high demand" 무한 반복
- 사용자 지시 "Gemini는 스킵" 받고 진행

**Phase 2 — 코드 강건성 강화**
- `pipeline/summarize.js`: 모델 fallback chain (gemini-2.5-flash → 2.0-flash → 2.0-flash-lite), retries 5회 baseDelay 2s
- 검증: 1.5-flash 계열은 v1beta API에서 404 NOT_FOUND
- URL fix: Anthropic RSS 5개 후보 모두 404 → 비활성, Sandhill `/rss` → `/feed`

**Phase 3 — 큐레이션 정정 (ADR-0003)**
- 사용자가 진짜 의도 명시: X 8개 + 뉴스레터 3개 = 11개
- SPEC.md 표(9개) 사용자 의도와 달랐음 → SPEC.md 갱신, sources.js 재구성
- `docs/decisions/0003-curate-eleven-sources.md` 작성

**Phase 4 — RSSHub 미러 발견**
- `rsshub.app`(메인) X 차단 → 8개 후보 중 `rsshub.pseudoyu.com`이 11/11 정상 응답
- `docs/runbooks/rsshub-self-host.md`에 "0단계: 검증된 공개 미러" 섹션 추가 (카드 등록 X)
- `~/.claude/projects/.../memory/reference_rsshub_mirror.md` 메모리 저장

**Phase 5 — 다양성·노이즈·UX 반복 개선 (사용자 피드백 6회)**
| 메일 # | 변경 | 결과 |
|---|---|---|
| 1 (`9865d5e7`) | 옛 SPEC 9개 소스 | 4건 |
| 2 (`25bf54e7`) | 11개로 정정, X 차단 | 3건/1 source |
| 3 (`d0e1fc83`) | 미러 발견, X 살아남, GeekNews 도배 | 13건/1 source |
| 4 (`66137a54`) | source당 cap 5 + 24h 보충 | 8건/4 source |
| 5 (`d09df947`) | self-promo 필터 + 72h, minSources 6 | 10건/6 source |
| 6 (`caec91c1`) | 회사 공식 우선 보충, minSources 8 | 12건/8 source |
| 7 (`703b5974`) | URL-only RT 글로벌 필터 | 12건/8 source, 노이즈 4건 제거 |
| 8 (`067ac3d3`) | title 끝 `...` 제거 + content 본문 추가 + 디자인 분리 강화 | 12건/8 source, X 트윗 1258자 풀 표시 |

### 결정
- **diversify 파라미터 default**: perSourceCap 5, minSources 8, lookbackHours 72 — 11개 중 8개 source 보장, 회사 공식 X 채널 우선 보충 (PRIORITY_SOURCES)
- **Gemini fallback chain**: 2.5-flash → 2.0-flash → 2.0-flash-lite (1.5-flash는 v1beta deprecated)
- **노이즈 필터 2단계**: 글로벌(URL-only RT, RT 헤더+URL 제거 후 30자 미만) + source-specific(GeekNews self-promo)
- **send-raw 본문 추가**: title 끝 잘림 제거 + content를 white-space:pre-wrap으로 풀 표시. title이 content prefix면 중복 제거.
- **render.js (production)는 미수정** — Gemini가 별도 `summary` 필드 생성하므로 잘림 없을 가능성. 첫 cron 발송 결과 보고 결정.

### 발견
- **RSSHub 공개 미러는 메인(rsshub.app)이 사실상 죽고, `rsshub.pseudoyu.com` 같은 개인 운영 미러가 살아있음** — reference 메모리 저장
- **Gemini 2.5/2.0 503 "high demand" 빈번** — free tier에선 시간대 따라 거의 모든 호출 실패. fallback chain 필수.
- **Claude(@claudeai) 5일 전, Claude Code(@claude_code) 3주 전이 최신 트윗** — 이 채널들은 거의 죽음. 72h lookback에도 안 들어옴 (어쩔 수 없음, 받아들임).
- **Resend 테스트 모드(`onboarding@resend.dev`)는 Resend 가입 이메일에만 발송 가능** — production은 도메인 인증 후 `MAIL_FROM` 자체 도메인으로
- **Gmail 등 메일 클라이언트가 글 사이 시각적 분리 약하면 source 라벨이 합쳐 보임** ("GJourney" 같이) — 우리 데이터는 정상, padding/border 강화로 완화

### 변경 파일 (Phase 1~5 누적)
- pipeline/config/sources.js (11개로 재구성)
- pipeline/summarize.js (Gemini fallback chain)
- pipeline/lib/window.js (신규, main에서 분리)
- pipeline/lib/diversify.js (신규, 다양성 보장 + PRIORITY_SOURCES)
- pipeline/lib/filter.js (신규, 노이즈 필터)
- pipeline/main.js (filterNoise + diversify 단계 추가)
- scripts/inspect-collect.js (신규)
- scripts/send-raw.js (신규, 점진 개선)
- SPEC.md (소스 표 갱신)
- docs/decisions/0003-curate-eleven-sources.md (신규)
- docs/runbooks/rsshub-self-host.md (신규, 공개 미러 + self-host 가이드)
- CHANGELOG.md (변경사항 누적)
- ~/.claude/projects/.../memory/reference_rsshub_mirror.md (신규)

### 커밋 (이번 세션 중 추가)
- `f06b387~6b11492`: 초기 셋업 4건 (이전)
- `55d0185` fix(pipeline): 첫 실 운영 검증 결과 반영
- `d63b5dc` feat(pipeline): 큐레이션 11개 + ADR-0003
- `9e3db36` docs(runbook): 공개 RSSHub 미러 우선 가이드
- `56ffb6f` feat(pipeline): 다양성 보장 (cap + 보충)
- `49825f4` feat(pipeline): 노이즈 필터 + 우선순위 보충
- `b460739` feat(filter): URL-only RT 글로벌 필터
- `51a0c7d` feat(send-raw): title 잘림 제거 + content 본문

### 다음 세션 액션
- [ ] **GitHub push + Netlify 배포** — `docs/runbooks/first-deployment.md` 4~7단계
  - GitHub Secrets에 `RSSHUB_BASE_URL=https://rsshub.pseudoyu.com` 포함
- [ ] **첫 실 cron 발송 모니터링** — Gemini 503 fallback이 실제로 동작하는지, 메일 디자인이 의도대로인지
- [ ] **render.js (production) 정리 검토** — Gemini summary 받은 후 잘림 발생하는지 첫 발송 결과 보고 결정
- [ ] **Claude/Claude Code X 채널 활성도 모니터링** — 계속 안 들어오면 sources.js에서 빼는 것도 옵션 (또는 그냥 받아들임)
- [ ] **(장기) Resend 도메인 인증** — `MAIL_FROM`을 자체 도메인으로 (현재는 `onboarding@resend.dev`)

### 변경 파일
- `.gitignore` — `.env` 계열 + `.claude/settings.local.json` 추가
- `.claude/settings.json` (신규)
- `.claude/hooks/scan-vite-secrets.sh` (신규, +x)
- `.claude/statusline.sh` (신규, +x)
- `.claude/commands/{log-session,new-adr,test-pipeline}.md` (신규)
- `.claude/agents/secret-auditor.md` (신규)
- `docs/README.md`, `docs/decisions/{README,0001-...}.md`, `docs/sessions/{README,2026-04-13}.md`, `docs/runbooks/{README,resend-quota-exceeded,rsshub-down}.md` (신규)
- `CHANGELOG.md` (신규)
